# React Native's Metro bundler cannot resolve pnpm's nested symlink layout.
# Expo's own monorepo guidance is to hoist; do not change this without testing
# a native build on a device.
node-linker=hoisted
